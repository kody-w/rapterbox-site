// RapterBox waitlist endpoint. Empty = the form submits to Formspree as before (no confirmations).
// Paste the Apps Script web-app URL from waitlist/README.md step 5 between the quotes and push.
window.WAITLIST_ENDPOINT = '';
